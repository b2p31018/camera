<% if @item.errors.any? %>
  <div id="error_item_explanation">
    <div class="alert alert-danger">
      入力エラーが<%= @item.errors.count %>件ありました。
    </div>
    <ul>
    <% @item.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
